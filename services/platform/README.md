# Platform Service

This service will host:

- MCP `tools/list` and `tools/call`
- wallet and catalog backend APIs
- tool execution router
- credential loading and billing logs

Recommended runtime: Python 3.11+ with FastAPI and official MCP Python SDK.
