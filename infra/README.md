# Infra

Infrastructure and migration resources for local/demo environments.

Contents:

- `alembic/` migration scripts
- `alembic.ini` Alembic config entrypoint
- `docker-compose.yml` for local service wiring
- `Dockerfile.backend` platform service image
- `Dockerfile.integrator` integrator service image
